#lang racket/base

(require "func.rkt")
(require rackunit)

; put <on> <type> <element>
; get <on> <type>

; persons --- index name
; maybe have some tags such as address, salary

(define (get-record person file-of-persons)
  ; 
)

(define (get-salary person file-of-persons)
  ;
)

(define (find-employee-record person file-of-persons)
  ;
)

